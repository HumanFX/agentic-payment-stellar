# Screenshots — TODO before submission

Capture from the demo UI and drop here (PNG, ~1600px wide, light theme):

| File | What to capture |
|---|---|
| `chat-compose-route.png` | Hosted agent §2 — the composed VND→USDC→BRL route reply with the alternatives table expanded |
| `confirm-card.png` | Hosted agent §3 — hold-to-confirm authorization card |
| `route-executing.png` | Hosted agent §5 — live per-leg progress with "USDC delivered to your wallet" |
| `intent-tracker.png` | Hosted agent §6 — settlement timeline with timestamped steps |
| `agent-management.png` | Self-hosted agent §1 — scoped key + mandate creation |
| `mcp-terminal-blocked.png` | Self-hosted agent §5 — the BLOCKED unregistered-recipient refusal in the terminal |
| `swap-form.png` | On-chain XLM↔USDC swap on testnet |

Also: `demo-thumbnail.png` for the video link in the README.

Redact before committing: real emails, JWTs, API keys, non-demo account
names, any non-testnet address.
